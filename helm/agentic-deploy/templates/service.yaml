apiVersion: v1
kind: Service
metadata:
  name: agentic-deploy
spec:
  type: {{ .Values.service.type }}
  selector: { app: agentic-deploy }
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
